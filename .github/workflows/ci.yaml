name: CI

on:
  push:
    branches:
      - dev
      - test
    paths:
      - app/backend/**
      - app/frontend/**
  workflow_dispatch:
    inputs:
      environment:
        description: "Target Environment"
        type: choice
        options:
          - dev
          - test
          - staging
          - perf
          - production
        default: dev
      build_frontend:
        description: "Build Frontend"
        type: boolean
        default: true
      build_backend:
        description: "Build Backend"
        type: boolean
        default: true
      aws_region:
        description: "AWS Region"
        type: choice
        options:
          - us-east-1
          - us-east-2
        default: us-east-1
      
permissions: 
  id-token: write 
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  ECR_REGISTRY: 385551094956.dkr.ecr.us-east-1.amazonaws.com
  BACKEND_IMAGE: app-backend
  FRONTEND_IMAGE: app-frontend

jobs:
  resolve-branch:
    name: Resolve Target Branch
    runs-on: ubuntu-latest
    outputs:
      branch: ${{ steps.resolve.outputs.branch }}

    steps:
      - name: Resolve branch from environment
        id: resolve
        run: |
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            case "${{ inputs.environment }}" in
              production) echo "branch=main" >> "$GITHUB_OUTPUT" ;;
            *)          
              echo "branch=${{ inputs.environment }}" >> "$GITHUB_OUTPUT" ;;
            esac
          else
            echo "branch=${{ github.ref_name }}" >> "$GITHUB_OUTPUT"
          fi

  lint-and-test:
    name: Lint & Test
    needs: resolve-branch
    runs-on: ubuntu-latest
    strategy:
      matrix:
        app: 
          - backend
          - frontend

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.resolve-branch.outputs.branch }}

      - name: Setup Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install uv
        run: pip install uv

      - name: Install dependencies
        working-directory: app/${{ matrix.app }}
        run: uv sync --frozen

      - name: Install lint/test tools
        run: pip install ruff pytest

      - name: Lint with Ruff
        working-directory: app/${{ matrix.app }}
        run: ruff check --output-format=github

      - name: Test with pytest
        working-directory: app/${{ matrix.app }}
        run: python -m pytest -v --tb=short || echo "No tests found"

  build-and-push:
    name: Build & Push Docker Images
    needs: 
      - lint-and-test
      - resolve-branch
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}

    strategy:
      matrix:
        include:
          - app: backend
            context: app/backend
            image: app-backend
            changed: backend

          - app: frontend
            context: app/frontend
            image: app-frontend
            changed: frontend
    
    outputs:
      image_tag: ${{ steps.tag.outputs.IMAGE_TAG }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.resolve-branch.outputs.branch }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::${{ vars.AWS_ACCOUNT_ID }}:role/github-actions-role
          aws-region: ${{ inputs.aws_region }}

      - name: Login to Amazon ECR
        uses: aws-actions/amazon-ecr-login@v2

      - name: Generate image tag
        id: tag
        run: echo "IMAGE_TAG=v$(date +'%Y%m%d')-$(echo ${{ github.sha }} | cut -c1-7)" >> "$GITHUB_OUTPUT"

      - name: Build Docker image
        uses: docker/build-push-action@v6
        with:
          context: ${{ matrix.context }}
          load: true
          platforms: linux/arm64
          tags: |
            ${{ matrix.image }}:${{ steps.tag.outputs.IMAGE_TAG }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
      
      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: ${{ matrix.image }}:${{ steps.tag.outputs.IMAGE_TAG }}
          format: table
          exit-code: 1
          ignore-unfixed: true
          severity: CRITICAL,HIGH

      - name: Push Docker image
        uses: docker/build-push-action@v6
        with:
          context: ${{ matrix.context }}
          push: true
          platforms: linux/arm64
          tags: |
            ${{ env.ECR_REGISTRY }}/${{ matrix.image }}:${{ steps.tag.outputs.IMAGE_TAG }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  update-values:
    name: Update GitOps Values
    needs: 
      - build-and-push
      - resolve-branch
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.resolve-branch.outputs.branch }}

      - name: Generate image tag
        id: tag
        run: echo "IMAGE_TAG=v$(date +'%Y%m%d')-$(echo ${{ github.sha }} | cut -c1-7)" >> "$GITHUB_OUTPUT"

      - name: Update backend values
        if: ${{ github.event_name != 'workflow_dispatch' || inputs.build_backend }}
        run: |
          ENV="${{ inputs.environment || github.ref_name }}"
          if [[ "$ENV" == "production" ]]; then
            yq -i '.image.tag = "${{ steps.tag.outputs.IMAGE_TAG }}"' gitops/production/us-east-1/backend/values.yaml
            yq -i '.image.tag = "${{ steps.tag.outputs.IMAGE_TAG }}"' gitops/production/us-east-2/backend/values.yaml
          else
            yq -i '.image.tag = "${{ steps.tag.outputs.IMAGE_TAG }}"' gitops/$ENV/backend/values.yaml
          fi

      - name: Update frontend values
        if: ${{ github.event_name != 'workflow_dispatch' || inputs.build_frontend }}
        run: |
          ENV="${{ inputs.environment || github.ref_name }}"
          if [[ "$ENV" == "production" ]]; then
            yq -i '.image.tag = "${{ steps.tag.outputs.IMAGE_TAG }}"' gitops/production/us-east-1/frontend/values.yaml
            yq -i '.image.tag = "${{ steps.tag.outputs.IMAGE_TAG }}"' gitops/production/us-east-2/frontend/values.yaml
          else
            yq -i '.image.tag = "${{ steps.tag.outputs.IMAGE_TAG }}"' gitops/$ENV/frontend/values.yaml
          fi

      - name: Direct commit to branch
        if: ${{ needs.resolve-branch.outputs.branch == 'dev' || needs.resolve-branch.outputs.branch == 'test' }}
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_user_name: "CI Bot"
          commit_user_email: "ci-bot@company.com"
          commit_message: "chore: update image tags to ${{ steps.tag.outputs.IMAGE_TAG }}"
          branch: ${{ needs.resolve-branch.outputs.branch }}
          file_pattern: gitops/

      - name: Create Pull Request
        if: ${{ needs.resolve-branch.outputs.branch == 'staging' || needs.resolve-branch.outputs.branch == 'perf' || needs.resolve-branch.outputs.branch == 'main' }}
        uses: peter-evans/create-pull-request@v7
        with:
          commit-message: "chore: update image tags to ${{ steps.tag.outputs.IMAGE_TAG }}"
          committer: "github-actions[bot] <github-actions[bot]@users.noreply.github.com>"
          author: "github-actions[bot] <github-actions[bot]@users.noreply.github.com>"
          branch: ci/update-tags-${{ steps.tag.outputs.IMAGE_TAG }}
          base: ${{ needs.resolve-branch.outputs.branch }}
          title: "chore: update image tags to ${{ steps.tag.outputs.IMAGE_TAG }}"
          body: |
            Automated image tag update triggered by CI.

            | Component | Image |
            |---|---|
            | Backend | `${{ env.ECR_REGISTRY }}/${{ env.BACKEND_IMAGE }}:${{ steps.tag.outputs.IMAGE_TAG }}` |
            | Frontend | `${{ env.ECR_REGISTRY }}/${{ env.FRONTEND_IMAGE }}:${{ steps.tag.outputs.IMAGE_TAG }}` |

            After merging, run the [CD workflow](${{ github.server_url }}/${{ github.repository }}/actions/workflows/cd.yaml) to deploy and promote.
          labels: automated-deploy
          delete-branch: true
