apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: frontend
  namespace: argocd
spec:
  generators:
    - list:
        elements:
          - env: production
            branch: main

  template:
    metadata:
      name: frontend-{{ env }}
      namespace: argocd
    spec:
      project: default

      source:
        repoURL: https://github.com/RaymonSun1230/my-devops-project.git
        targetRevision: "{{ branch }}"
        path: helm-charts/frontend
        helm:
          valueFiles:
            - $values/gitops/{{ env }}/frontend/values.yaml

      destination:
        server: https://kubernetes.default.svc
        namespace: myapp-{{ env }}
